keys:
  # User SSH key for all secrets
  - &user_ssh ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICQ5a7p+8LrnKTch/UUAJ3YpAYT6PS8fM+0FKtSspZ5U fjimenez@vfjimenez 

creation_rules:
  # All secrets use user SSH key
  - path_regex: 'secrets/.*\.(yaml|env)$'
    ssh: *user_ssh

  # NetBird setup key binary file  
  - path_regex: 'secrets/netbird-setup-key$'
    ssh: *user_ssh
